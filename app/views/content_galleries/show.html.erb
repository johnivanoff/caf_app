<p id="notice"><%= notice %></p>

  <%= image_tag @content_gallery.image.url(:directory_size).to_s %>


<p>
  <b>Caption:</b>
  <%= @content_gallery.caption %>
</p>

<p>
  <b>Photographer:</b>
  <%= @content_gallery.photographer %>
</p>

        <table>
          <tr><td>original</td><td><%= @content_gallery.image.url %></td></tr>
          <tr><td>265 x 265 square</td><td><%= @content_gallery.image.url(:directory_size).to_s %></td></tr>
          <tr><td>50 x 50 square</td><td><%= @content_gallery.image.url(:thumb).to_s %></td></tr>
          <tr><td>35 x 35 square</td><td><%= @content_gallery.image.url(:tiny_thumb).to_s %></td></tr>
        </table>

<%= link_to 'Edit', edit_content_gallery_path(@content_gallery) %> |
<%= link_to 'Back', content_galleries_path %>
