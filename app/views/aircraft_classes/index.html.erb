<h1>Aircraft Classes</h1>

<table>
  <tr>
    <th>Class type</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @aircraft_classes.each do |aircraft_class| %>
  <tr>
    <td><%= aircraft_class.class_type %></td>
    <td><%= aircraft_class.description %></td>
    <td><%= link_to 'Show', aircraft_class %></td>
    <td><%= link_to 'Edit', edit_aircraft_class_path(aircraft_class) %></td>
    <td><%= link_to 'Destroy', aircraft_class, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Aircraft class', new_aircraft_class_path %>
