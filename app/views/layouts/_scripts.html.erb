<script>

$.fn.scrollView = function () {
    return this.each(function () {
        $('html, body').animate({
            scrollTop: $(this).offset().top
        }, 1000);
    });
}


$(function() {
  $('th').click(function(){
      var table = $(this).parents('table').eq(0)
      var rows = table.find('tr:gt(0)').toArray().sort(comparer($(this).index()))
      this.asc = !this.asc
      if (!this.asc){rows = rows.reverse()}
      for (var i = 0; i < rows.length; i++){table.append(rows[i])}
  })
function comparer(index) {
    return function(a, b) {
        var valA = getCellValue(a, index), valB = getCellValue(b, index)
        return $.isNumeric(valA) && $.isNumeric(valB) ? valA - valB : valA.localeCompare(valB)
    }
}
function getCellValue(row, index){ return $(row).children('td').eq(index).html() }
  });


// filter clicks

$(function() {
  $('#Bomber').click(function(){
        document.getElementById("filter").value = "Bomber";
				$("#filter").keyup();
//				var elem = document.getElementById("ac-class");
//				elem.style.marginBottom = "500px";
				$("#live-search").scrollView();
    });
  $('#Fighter').click(function(){
        document.getElementById("filter").value = "Fighter";
				$("#filter").keyup();
				$("#live-search").scrollView();
    });
  $('#Trainer').click(function(){
        document.getElementById("filter").value = "Trainer";
				$("#filter").keyup();
				$("#live-search").scrollView();
    });
  $('#Transport').click(function(){
        document.getElementById("filter").value = "Transport";
				$("#filter").keyup();
				$("#live-search").scrollView();
    });
  $('#Liason').click(function(){
        document.getElementById("filter").value = "Liason";
				$("#filter").keyup();
				$("#live-search").scrollView();
    });
  $('#Foreign').click(function(){
        document.getElementById("filter").value = "Foreign";
				$("#filter").keyup();
				$("#live-search").scrollView();
    });
  });
  
</script>