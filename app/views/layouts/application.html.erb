<!DOCTYPE html>
<html>
<head>
  <title>Commemorative Air Force</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<% if user_signed_in? %>
    <%= link_to current_user.member.first_name + ' ' + current_user.member.last_name, member_path(current_user.member) %> | 
    <%= link_to 'Logout', destroy_user_session_path, :method => :delete %>
<% end %>


<p id="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>


<%= yield %>

</body>
</html>
